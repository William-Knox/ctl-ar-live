# One Hyphen review chrome — Option A mock

2026-08-20. Frame. HTML mock exists. Not live. data.js source untouched.

Dominus skipped A/B/C word sketches. He locked **Option A** (table + split). B and C were not built.

## Open

- http://127.0.0.1:8766/a/
- file:///workspace/hyphen-mocks/review-options-2026-08-20/a/index.html
- Shared: `styles.css`, `app.js`, `data.js` (copy), `ctl-mark-on-white.png`
- Mirror: `/workspace/hyphen-board/pages-host/review-options/a/` (does not touch live hyphen files)

## Locked

- Left list = current filtered queue across every box. Not address/PO family.
- leftover-case.html is not a product. Leftover is an Issue + a filter chip (set 18).
- Open any invoice → one full-page review. Next row → same UI.
- No leftover-review chrome. No Chase side drawer.
- Hide Timeline of Events when there is no ledger. Do not invent a Today leftover row.
- PTA: no Draft, no Send now. Hold / Next only.
- Write-off: Esme note (william@ → Esmeralda). No builder letter. Compact Put in Drafts if analysis exists.
- Awaiting: Hold / Next only. Show already-sent letter. Do not rewrite.
- Empty analysis: Hold. Do not invent.
- Select all next to Clear. Brand-kit. Signed ledger labels. Clay on Today / past-due days only.
- Boxes switch which queue is in the left list. Case chrome does not change.
- Left table columns: Days, Invoice, Address, Issue, Open $ (Mail is not a column).

## A — table + split (this mock)

LEFT (~420px table): current filtered queue.

REST:
- Top strip: invoice · address · parent · open $ · days. Prev / Next.
- Analysis paragraph.
- Two columns: ledger (if any) | letter or Esme or attachments (if any).
- Hide the ledger column when empty. Hide the letter column when no letter and no attachments.
- One remaining column goes full width of the case.

Starter: 0430-171077.
